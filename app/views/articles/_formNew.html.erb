<h1><%= name ||= "Crear"%> articulo</h1>
<%= form_for(@article) do |f| %>




<%= @article.errors.full_messages.each do |message|%>
<div class="redbe whiteFont spacetap">
    * <%= message %>
</div>
<% end %>

   <!--<%= @article.errors.full_messages.each do |message| %>
    <%= javascript_tag "Materialize.toast('#{message}', 4000)" %>

<%end%>-->

    <div class="field">
        <%= f.text_field :title, placeholder: "Titulo",  id: "valida"%>
    </div>
    <div class="field">
    <%= f.text_area :body, placeholder: "Escribe aqui el articulo", style: "height:200px;" %>
    
    </div>
    <!-- <button class="btn waves-effect waves-light floating" type="submit" name="action">Guardar
    <i class="material-icons right">send</i>
  </button>-->
      <%= f.submit "Guardare", class:"btn waves-effect waves-light" %>
<% end %>